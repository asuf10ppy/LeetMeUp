
class Solution:
    def minimumPrefixLength(self, nums: List[int]) -> int:
        nums.reverse()
        for i in range(len(nums)-1):
            if nums[i+1]>=nums[i]:
                return len(nums)-i-1
        return 0