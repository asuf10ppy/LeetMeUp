
class Solution:
    def countBits(self, n: int) -> list[int]:
        ans = [0] * (n + 1)
        
        for i in range(1, n + 1):
            half = i // 2
            remainder = i % 2
            ans[i] = ans[half] + remainder
            
        return ans
